Write a program to print Fibonacci series using recursion 


def fibonacci(n):

    if n <= 0:

        return []

    elif n == 1:

        return [0]

    elif n == 2:

        return [0, 1]

    else:


  sequence = fibonacci(n - 1)

        sequence.append(sequence[-1] + sequence[-2])

        return sequence
num = int(input( "enter the no. of elements in the fibanacci series"))

fib_series = Fibonacci (num)

  

print("Fibonacci Series up to",num,"elements", fib_series)

   

       






